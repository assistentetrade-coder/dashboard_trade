# dashboard_trade

Projeto exportado: dashboard-trade-marketing-pre-carregado (1).html

Arquivos principais:
- dashboard-trade-marketing-pre-carregado (1).html

Como subir:
1. `git init`
2. `git add .`
3. `git commit -m "Initial commit"`
4. Criar repositório no GitHub e `git push`.
